# Use GitHub as the canonical HString source

HString has several configured Git remotes, so the learning site will treat the GitHub repository and a pinned commit as its only canonical source. This keeps source links and explanations stable while the other remotes remain transport or backup locations rather than separate content authorities.
